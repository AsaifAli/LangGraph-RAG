# EvidenceFlow dependencies: LangGraph + OpenSearch sparse-first retrieval
# and a local cross-encoder reranker. No dense vector database is required.

langgraph==1.2.9
langgraph-checkpoint-sqlite==3.1.1

# Chat model backends
langchain-google-genai==4.3.2
langchain-openai==1.1.0
langchain-huggingface==1.2.2
huggingface_hub==1.26.1

# Retrieval / indexing
opensearch-py==2.8.0
langchain-text-splitters==1.1.2

# Local reranking only; this is not used for vector retrieval.
fastembed==0.8.0

# Provider / API utilities
tavily-python==0.7.27
google-genai==2.17.0
pydantic==2.13.4
python-dotenv==1.2.2

# Document extraction
pdfplumber==0.11.9
python-docx==1.2.0

# Data analysis demo
pandas==3.0.5
openpyxl==3.1.5

# UI
streamlit==1.61.1

# Tests / tooling
pytest==8.4.2
pytest-asyncio==0.26.0
pytest-cov==6.0.0
setuptools>=75

# Optional primary-source MCP client
mcp>=2,<3
